<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Trouble - Swizz Times</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        body {
            background-color: rgb(69, 65, 65);
            color: white;
        }
        .custom-card {
            background-color: #18181b;
            border: 1px solid #27272a;
        }
        .custom-input {
            background-color: #27272a;
            border: 1px solid #3f3f46;
            color: white;
        }
        .custom-input::placeholder {
            color: #9ca3af;
        }
    </style>
</head>
<body class="d-flex flex-column align-items-center justify-content-center min-vh-100 p-4">
    
    <!-- Header -->
    <div class="w-100 max-w-md d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold">Swizz Times</h1>
        <div>
            <a href="/login" class="text-white me-3">Log In</a>
            <a href="/signup" class="btn btn-primary">Sign Up</a>
        </div>
    </div>
    
    <!-- Main Card -->
    <div class="card custom-card w-25 max-w-md p-4">
        <div class="card-body text-center">
            <h2 class="fw-bold ">Trouble logging in?</h2>
            <p class="text-secondary">Enter your email related with the SWIZZ account and we'll send you an OTP to change your password.</p>
            
            <!-- Form for sending OTP -->
            <form action="/forgot-password" method="POST">
                <input type="email" name="email" class="form-control custom-input mb-3" placeholder="Enter your email here" required>
                
                <button type="submit" class="btn btn-primary w-100 mb-3">Send OTP</button>
            </form>
            
            <div class="d-flex align-items-center my-3">
                <hr class="flex-grow-1 text-secondary">
                <span class="mx-2 text-secondary">OR</span>
                <hr class="flex-grow-1 text-secondary">
            </div>
            
            <a href="/signup" class="text-primary">Create new account</a>
        </div>
    </div>

    <!-- Footer -->
    <a href="/login" class="mt-3 text-white">Back to login</a>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
