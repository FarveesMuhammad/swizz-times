<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        :root {
            --primary-color: #3498db;
            --secondary-color: #2ecc71;
            --background-color: #f4f6f9;
            --text-color: #2c3e50;
            --muted-color: #7f8c8d;
        }

        * {
            transition: all 0.3s ease;
        }

        body {
            background-color: var(--background-color);
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            color: var(--text-color);
        }

        .order-confirmation-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .order-confirmation-card {
            background-color: white;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(50, 50, 93, 0.1), 0 5px 15px rgba(0, 0, 0, 0.07);
            max-width: 900px;
            width: 100%;
            overflow: hidden;
            display: flex;
            animation: fadeIn 0.6s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .confirmation-illustration {
            flex: 1;
            background: linear-gradient(135deg, var(--primary-color), #2980b9);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: white;
            padding: 40px;
            text-align: center;
        }

        .confirmation-illustration i {
            font-size: 100px;
            margin-bottom: 20px;
            animation: bounce 1.5s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .confirmation-details {
            flex: 1.5;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .order-number {
            background-color: rgba(52, 152, 219, 0.1);
            color: var(--primary-color);
            padding: 8px 15px;
            border-radius: 50px;
            display: inline-block;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .tracking-details {
            background-color: #f8f9fa;
            border-radius: 12px;
            padding: 20px;
            margin-top: 20px;
        }

        .btn-primary-custom {
            background-color: var(--primary-color);
            border: none;
            padding: 12px 25px;
            border-radius: 8px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .btn-primary-custom:hover {
            background-color: #2980b9;
            transform: translateY(-3px);
            box-shadow: 0 7px 14px rgba(50, 50, 93, 0.1), 0 3px 6px rgba(0, 0, 0, 0.08);
        }

        .btn-outline-custom {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            background-color: transparent;
            padding: 12px 25px;
            border-radius: 8px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .btn-outline-custom:hover {
            background-color: rgba(52, 152, 219, 0.1);
        }

        @media (max-width: 768px) {
            .order-confirmation-card {
                flex-direction: column;
            }

            .confirmation-illustration, .confirmation-details {
                flex: 1;
                padding: 30px;
            }
        }
    </style>
</head>
<body>
    <div class="order-confirmation-container">
        <div class="order-confirmation-card">
            <div class="confirmation-illustration">
                <i class="fas fa-check-circle text-white"></i>
                <h2 class="mb-3">Order Confirmed!</h2>
                <p>Thank you for your purchase. Your order is being processed.</p>
            </div>
            
            <div class="confirmation-details">
                <span class="order-number"><%= order.orderId %></span>
                
                <h3 class="mb-4">Order Summary</h3>
                
                <div class="tracking-details">
                    <div class="d-flex justify-content-between mb-3">
                        <span class="text-muted">Order Date:</span>
                        <span class="fw-bold"><%= order.createdAt %></span>
                    </div>
                    <div class="d-flex justify-content-between mb-3">
                        <span class="text-muted">Estimated Delivery:</span>
                        <span class="fw-bold text-primary"><%= order.deliveryDate %></span>
                    </div>
                    <div class="d-flex justify-content-between">
                        <span class="text-muted">Order Status:</span>
                        <span class="fw-bold text-success"><%= order.orderStatus %></span>
                    </div>
                </div>
                
                <div class="d-grid gap-3 mt-4">
                    <a href="/order-details/<%= order._id %>" class="btn btn-primary-custom">
                        <i class="fas fa-receipt"></i> View Order Details
                    </a>
                    <a href="/shop" class="btn btn-outline-custom">
                        <i class="fas fa-shopping-cart"></i> Continue Shopping
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>